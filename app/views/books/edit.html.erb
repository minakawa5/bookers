<body>
<h1>Edit Book</h1>

<%= form_with model: @book, url: book_path(@book.id), method: :patch do |book| %>

<div class="field">
  <div>title</div>
  <%= book.text_field :title %>
</div>  
 
<div class="field"> 
  <div>body</div>
  <%= book.text_area :body %>
</div>
  
  <div class=actions><%= book.submit 'Update Book' %></div>
  
  <%= link_to "Show", book_path(@book.id) %>
  |
 <%= link_to "Back", books_path %>
  
<% end %>
</body>